"""Multi-provider LLM client (Gemini + OpenAI-compatible APIs)."""
from __future__ import annotations

import logging
from typing import Any, List, Optional

logger = logging.getLogger("sc2_chatbot.llm")

# PLACEHOLDER - will be fixed
class LLMClient:
    def __init__(self, *, provider="gemini", api_key="", model="gemini-2.0-flash", temperature=0.85, max_tokens=180, base_url=None):
        raise RuntimeError("llm_client was corrupted during update; restoring")
