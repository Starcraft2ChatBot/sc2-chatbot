google-generativeai>=0.8.0
pyyaml>=6.0
pydantic>=2.0
rich>=13.0
python-dotenv>=1.0
aiofiles>=23.0

# Optional – only needed for the live SC2 stub backend
pyautogui>=0.9.54
PyGetWindow>=0.0.9
mss>=9.0.0
Pillow>=10.0.0
pytesseract>=0.3.10
# pywin32          # recommended on Windows for better window handling

# Optional – only needed to BUILD the portable folder
# pyinstaller>=6.0
